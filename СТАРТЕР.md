# СТАРТЕР — вставити першим повідомленням у нову розмову

> **ЧЕКАЄМО НА ФАЙЛИ · 21.08 близько 11:00.** Власник обіцяв експорти з
> інших моніторів — John Deere, Claas, New Holland, можливо Case. Доки
> формат один, «універсальний двигун» — це слово, а не властивість.
> **Нагадати про це в першому ж повідомленні.**

Працюємо за системою Progressive Elaboration.

Порядок сесії:

1. Спершу прочитай `C:\WORK D\agroplace-farms\FieldFactor\DAILY_LOG.md` —
   верхній запис.
2. Далі `FieldFactor\STATE.md`, потім `_макети\ПЛАН_НОРМАЛІЗАЦІЇ.md` —
   це чинний план роботи, пʼять частин, погоджений у діалозі.
3. Один батч = одна задача. Не розповзаємось і не забігаємо наперед.
4. Наприкінці — запис у `DAILY_LOG.md` і перезапис `СТАРТЕР.md`.

**Пояснюй простою мовою.** Підсумок наприкінці батча — що зʼявилось у
програмі і навіщо це в полі, словами агронома. Назви модулів і механіка
перевірок — у `DAILY_LOG.md`, не в підсумку.

**Головне правило, і воно має три поверхи.**

1. Поки екран не побачений у браузері — він не зроблений. Клацати треба
   справжнім клацанням, а не викликом `.click()` з консолі.
2. **Зелений результат перевіряє щасливий шлях, червоний перевіряє
   перевірку.** Механізм тривоги, якого не бачили в стані тривоги, — це
   напис, а не механізм.
3. **Коли підкручування дає «майже» — це не привід підкрутити ще.** Це
   сигнал, що лікуєш наслідок. Зупинись і спитай, звідки береться різниця.

**Ціна пошуку називається до пошуку.** 20.08 я довго колупав чужу базу,
яка виявилась під паролем, замість сказати одразу: «шлях закритий,
пропоную інший». Уперся в замок — скажи вголос, не шукай обхід.

**Правка файлів — тільки точковими замінами.** Патчі клади окремим `.py`
(bash тут ламається на heredoc із кирилицею), після заміни перевіряй, що
зникло рівно те, що мало.

**Проєкт.** FieldFactor — портативна програма для агрономічного
консалтингу. Тека `C:\WORK D\agroplace-farms\FieldFactor\`, власний git,
репозиторій `Dragonfly-erp/Field-Factor`. Запуск: `ЗАПУСК.bat` → браузер
на `127.0.0.1:8756`. У `.bat` тільки латиниця. Конфіг запуску для писаря —
`C:\WORK D\agroplace-farms\.claude\launch.json`, записи `FieldFactor` і
`FieldFactor-8757`.

## Верхнє робоче меню — зроблено 20.08

```text
Нормалізація → Зонування → Відбір → Аналізи → Карта внесення → Звіти
                                          довідники: Продукти · Метод
```

Малюється одним файлом `app/web/меню.js` на всіх сторінках. **Документи** —
ще один пункт, погоджений, але не зроблений.

## Що стоїть у програмі

**`/нормалізація`** — нова сторінка. Поле й межа · файли монітора ·
розбір файлів із правкою колонок руками · культура, рік, залікова
вологість, натура · крок сітки · прогін із журналом · підсумок. Карта лягає
в дерево поля **з роком і культурою**.

**Зведення на сітку.** Монітор пише сотні тисяч записів; на виході —
17 106 точок на сітці 10 м. Межі кроку 10…25 м тримає і екран, і
розрахунок: 3 м на екрані відмовляють, повз екран — обрізають і пишуть
чому. Біля кожної точки — скільки записів за нею і як вони розходились.

**`/метод`** · **`/аналізи`** · **`/продукти`** · **`/відбір`** ·
**`/карта`** — як було. На `/карта` є **геометрія під монітор**: дрібні
шматки, спрощення меж, мінімальний розмір уздовж руху, буфер за межу.

**`/звіти`** — порожня сторінка, чесно підписана «у розробці».

## Робочі поля

- **`Зелена_Поляна\Г_03`** — справжнє поле, кукурудза 2025, межа від
  Власника 177,80 га, 222 760 записів монітора за 6 днів. Нормалізація
  проходить наскрізь за 42 секунди. **Це головне поле для перевірок.**
- `ДЕМО_тестові_дані\Долинка_12` — синтетика, 41,87 га.
- `Угринів\Гречка` — сканування RTK + EC, аналізів немає.

## Що знає програма про Г-03 (перевірено числами)

- монітор приводив врожайність до **15,00 %** вологості — відновлено з його
  ж колонок, згода 99,2 %;
- натура **0,7208 кг/л**, розкид у четвертому знаку — тобто обʼєм там не
  другий вимір, а маса, поділена на сталу з налаштувань;
- затримка сенсора **10 записів = 16 м**;
- вологість: шум 0,25 %, дрейф між днями 0,36 %, **сталого зсуву немає**;
- висота: розбіжність між днями **0,24 м** при перепаді поля 15 м;
- швидкість **не свідок**: між днями тримається місця на r = −0,21.

## Чинний план роботи

`_макети\ПЛАН_НОРМАЛІЗАЦІЇ.md` — пʼять частин. Головне з погодженого:

- **нормалізуємо всі дані**, не тільки врожайність; жодна колонка вихідного
  файлу не зникає;
- **модуль співставлення колонок обовʼязковий** і він же конструктор
  виходу: клієнт бачить, що знайдено і чому, та каже, що хоче в результаті;
  шаблон зберігається під підпис формату;
- **канонічні атрибути обовʼязкові до заповнення**, пишуться під нашими
  іменами; роль зашита в картку атрибута, а не у вибір клієнта;
- **словник атрибутів пишемо самі** — спроба взяти з SMS закрита, база під
  паролем;
- **свідок не призначається наперед** — перевіряється, чи тримається він
  місця; той самий тест дає стелю правки інтерполяції;
- **пропуски**: спершу інтерполяція як гіпотеза, потім правка вегетацією
  та яскравістю ґрунту як підтвердження. Виміряні числа індексом не
  правимо ніколи;
- **смугастість проходів тільки показуємо**, не правимо — правитимемо на
  етапі вивчення поля;
- **зупинки** (83 479 записів, 37 %) — у звіт і в «Документи», у дерево не
  потрапляють, у врожайність їхні числа не входять;
- **дата посіву** — нове питання, щоб брати яскравість ґрунту за 2–3 тижні
  до сівби.

## Наступні три задачі

1. Файли з інших моніторів → перевірка універсальності.
2. Модуль співставлення колонок (розділ 27 плану).
3. Розпізнавання колонок за самими числами (розділ 11, шар 2).

## Рішення, які не переобговорюються

- методи аналізу — тільки Bray, Olsen, Mehlich-3;
- заносимо той метод, який лабораторія зробила; придатність — окремо;
- **програма не питає того, що може виміряти**;
- норми звужені до вапнування; бенди й Ca:Mg:K — поза обсягом;
- точка = проба, зразок = багато проб або точок;
- карта каже, де брати зразок, а не скільки сипати;
- комірка відбору не накриває дві протилежні ділянки;
- у класичній сітці розмір каже замовник;
- на техніку йде тільки латиниця; назви господарств і полів у програмі
  лишаються кирилицею;
- не робимо: калібрування монітора по вазі окремим кроком, історію
  внесення, гній.

**Дані господарств у git не потрапляють ніколи.** У `.gitignore`:
`робота/`, `шаблони_лабораторій/`, `довідник_продуктів.json`.
