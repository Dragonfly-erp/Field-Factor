# СТАРТЕР — вставити першим повідомленням у нову розмову

> **ПЕРШЕ: прочитати `ПРАВИЛА_НОРМАЛІЗАЦІЇ.md`.** Це не довідка, це
> озброєння. Пороги там не вигадані — Yield Editor 2.0 (Sudduth & Drummond,
> 2007, USDA-ARS). **Своїх порогів не вигадуємо.** Треба інше число —
> знайди джерело й назви його.
>
> **ШАБЛОН НОРМАЛІЗАЦІЇ ЗАКРИТО 21.08.** Три файли на виході, три сторінки
> у звіті, Г-03 проходить наскрізь **одним прогоном**. Наступна сесія —
> **решта продукту**, не добудова звіту.
>
> **ПИТАННЯ, ЩО ЧЕКАЄ ВІДПОВІДІ:** `pipeline._текст_qc` збирала протокол
> очистки — один із двадцяти файлів, прибраних із віддачі 21.08. Її не
> кличе ніхто. Оживити чи прибрати разом із рештою?
>
> **ТЕКА ЗМІНИЛАСЬ 21.08.** Продукт живе в `C:\WORK D\FieldFactor` —
> сусід майстерні, не її частина. **Сесію відкривати саме там.**
>
> **МОНІТОРИ ВІДКЛАДЕНО.** Рішення Власника 21.08: «файли із моніторів поки
> забуваємо». А1 стоїть. Прийшли не експорти, а **картки терміналів**:
> John Deere тримає врожайність у своєму protobuf, New Holland — у двійковому
> `CNH1`, обидва закриті. Розібрано й записано в `ЧЕРГА.md` — **не переробляти**.

Працюємо за системою Progressive Elaboration.

Порядок сесії:

1. `FieldFactor\ПРАВИЛА_НОРМАЛІЗАЦІЇ.md` — правила й джерела.
2. `FieldFactor\DAILY_LOG.md` — верхній запис.
3. **`FieldFactor\ЧЕРГА.md`** — пріоритети А·Б·В. **Робота починається звідти.**
4. `FieldFactor\STATE.md`, далі `_макети\ПЛАН_НОРМАЛІЗАЦІЇ.md`.
5. Один батч = одна задача. Не розповзаємось.
6. Наприкінці — запис у `DAILY_LOG.md` і перезапис `СТАРТЕР.md`.

## Закриття сесії — чотири дії, без нагадувань

1. запис у `DAILY_LOG.md` — що зроблено · чому · **чого навчився** ·
   проблеми й рішення · план на завтра;
2. оновлення `ЧЕРГА.md`;
3. перезапис `СТАРТЕР.md` на диску;
4. **видати повний текст стартера в чат**.

## Правила, куплені дорого

**Перевіряй те, що віддаєш, а не те, що в пам'яті.** 21.08 округлення при
стисканні DBF обнулило 789 значень потоку. Таблиця в пам'яті лишалась
бездоганною, ворота казали «чисто», клієнт отримав би зіпсований файл.
Між пам'яттю й файлом стоять округлення, формат поля, довжина й кодування.

**Питай кожні ворота: що саме вони перевіряють?** Арифметика семи чисел SMS
ніколи не спиняла видачу — її результат ішов рядком у текст довідки.

**Те, що не кричить, не означає, що воно працює.** Розворотна смуга не
потрапляла у звіт узагалі: складальник не передавав контур. Жодної помилки,
блок просто мовчки не виконувався.

**Мірило перевіряй так само суворо, як механізм.** За один день тричі
винесено вирок на хибному мірилі: eta² на різній кількості груп, максимум
замість медіани, розкид/середнє як міра гомогенізації. Механізм щоразу
працював. Сторожа переповнення PDF — той самий випадок: вона дивилась на
курсор, а курсор не рухався.

**Перевіряй карту, а не тільки числа.** Усі перевірки проходили — ширина,
смугастість, арифметика до 10⁻¹³ — а карта виглядала шумом.

**Знаки після коми обираються від величини, а не від звички.** Три знаки на
числі порядку сотих — це один значущий розряд.

**Зелений результат перевіряє щасливий шлях, червоний перевіряє перевірку.**

**Коментар — теж код, і він теж може мовчки зʼїсти дані.** 21.08 незакритий
коментар у таблиці точності зʼїв чотири колонки: синтаксис правильний,
помилки немає, а клієнт отримував поля по 17 байтів із 15 знаками після
коми замість чотирьох.

**Ворота мусять КАЗАТИ, що не так, а не падати.** Виняток numpy — це теж
зупинка, але людина з нього нічого не дізнається.

**Виправити механізм мало — спитай, хто робить те саме повз нього.** Батч 14
навчив `малюнок` міряти картинку, а два виклики `p.image` повз нього лишились
жити зі старим здогадом `0,42` — і сторожа була до них так само сліпа.

**Число, узяте з одного поля, — це не константа, це здогад із іменем.**
`0,42` виглядало як параметр верстки. Це була форма Г-03.

**Коли з віддачі забирають файли — пройдись по всіх, хто на них
посилався.** 21.08 прибрали двадцять файлів і разом із ними непомітно
зламали дорогу від нормалізації до зонування: поле лишалось без шару
врожайності, бо `if файл є` мовчки не спрацьовував. **`if файл є` без
`else` — це не умова, це дірка.**

**Свій прогін заводь тим самим шляхом, що й людина.** Безголовий прогін без
`reading.розвідати` не знав, що монітор уже приводив урожайність до свого
базису, застосував формулу вологості вдруге й зсунув карту на 17 % — а
ворота видачі пройшли чисто. Врятувало тільки те, що число не збіглося з
учорашнім журналом.

**Ціна пошуку називається до пошуку.** Уперся в замок — скажи вголос.

**Правка файлів — тільки точковими замінами.** Патчі окремим `.py`.

**Пояснюй простою мовою.** Підсумок батча — словами агронома.

**Коротко, і завжди з орієнтацією.** Власник має з перших рядків бачити:
**де ми** (розділ і задача з `ЧЕРГА.md`), **що робимо**, **на що це впливає**
в продукті. Стіна тексту про хід роботи забирає в нього ситуацію, а не дає.
Подробиці процесу — у `DAILY_LOG.md`, не в чат.

**Ніяких шифрів у чаті.** «А0-Г.1», «Б5.2» — це закладки в черзі, а не мова
Власника: у нього вісім екранів тексту, і код задачі йому нічого не каже.
Називати **річ**: не «беремо А0-Г.1», а «перевіряю, чи ворота видачі дивляться
у файл, а не в памʼять». Код — щонайбільше в дужках наприкінці.

**Питання ставиться рівно чотирма рядками:** саме питання · звідки воно
виникло · на що воно впливає · моя рекомендація з причиною. Без цих чотирьох
питання не ставиться. Те, що можу вирішити сам, — вирішую сам.

## Проєкт

FieldFactor — портативна програма для агрономічного консалтингу.
`C:\WORK D\FieldFactor\` — сусід майстерні, не її частина. Власний git,
репозиторій `Dragonfly-erp/Field-Factor`. Запуск: `ЗАПУСК.bat` →
`127.0.0.1:8756`. У `.bat` тільки латиниця.

**Дві сесії на диску — це нормально, і тепер вони не перетинаються.**
21.08 продукт переїхав із `agroplace-farms/FieldFactor/` у власну теку
`C:\WORK D\FieldFactor\`: Власник працює в майстерні, я — у продукті.
Сесію FieldFactor **відкривати в `C:\WORK D\FieldFactor`**, не в майстерні.
У `engine/` і `farms/` не заходити.

## Що готове: шаблон нормалізації

**Видача — рівно три речі**, і нових файлів туди не додавати:

| | |
|---|---|
| shapefile | 32 атрибути, DBF 3,6 МБ на 17 102 комірки |
| `Звіт_нормалізації.pdf` | три сторінки |
| `SMS_import_guide.txt` | сім обов'язкових атрибутів + додаткові |

**Три сторінки звіту:**

1. початкова карта з монітора · нормалізована · легенда;
2. три свідки, не зняті комбайном: багаторічна вегетація, голий ґрунт,
   рельєф — зі згодою кожного з урожайністю;
3. умовно втрачені можливості · **карта «як могло б бути»** під нею ·
   таблиця по ділянках · розворотна смуга.

**Модель «як могло б бути» — три правила:** підйом рівний по всій ділянці
(медіанний недобір, не прогноз по комірці) · сума доданого дорівнює тоннам
у таблиці · ділянки «вище» не опускаються. Малюється в тих самих межах
класів, що й нормалізована карта.

## Картки моніторів — розібрано 21.08, робота відкладена

Сім архівів у `Downloads` — це **один масив** із різних рівнів вкладеності;
розпаковувати треба лише `КЛІЄНТИ_2025_2`. Господарство Svitanok.

Читається: **ISOXML — контури 28 полів** (ISO 11783) і **JD `MasterData.xml`**
(143 ферми, 142 поля, 111 культур із заліковою вологістю). Не читається:
`.jdl` (protobuf JD), `.tlh/.tlo/.tlt` (`CNH1`), `.agf` (зашифровано).
375 `.txt` у теці `log` — **не дані, а лог терміналу**.

Коли повернемось: у `C:\WORK D\SMS\ADAPT` уже стоять плагіни, які читають
ці формати й пишуть shapefile. Просити експорт із SMS дешевше, ніж розбирати
чужий двійковий формат.

## Стан Г-03

| | |
|---|---|
| робоча ширина з геометрії | 6,00 записана → **4,90 виміряна**, перекриття 18 % |
| смугастість через шов | 87 % → **10 %** |
| монітор проти вагової | **12 050 проти 12 128, −0,65 %** без ручного коефіцієнта |
| фільтри Yield Editor | 12 із 12, прибрано 16,2 % |
| гладка поверхня | радіус обирається сам, згода з рослинністю **0,743** |
| арифметика SMS | найгірша комірка **3,1·10⁻³**, перевіряється на записаному файлі |
| втрачені можливості | 4 ділянки, 14,9 га, **27 т** |
| три файли віддачі | складаються **одним прогоном**, ворота рахують сторінки в записаному PDF |
| розкладка звіту | висота картинок **вимірюється**; `.image()` дозволено тільки в `_Аркуш` |
| ворота видачі | shapefile **перечитується з диска** й звіряється покомірково з памʼяттю |
| шар поля | `шари\врожайність_2025.shp` реєструється після прогону — звідси бере зонування |

**Свідки:** вегетація сезону +0,669 · за 5 років +0,461 · рельєф +0,280 ·
голий ґрунт +0,041. Разом пояснюють 48 %.

## Що стоїть у програмі

`/нормалізація` · `/зонування` · `/відбір` · `/аналізи` · `/карта` ·
`/продукти` · `/метод`. `/звіти` — порожня.

**Модулі:** `qc.py` (12 фільтрів) · `passes.py` (проходи, робоча ширина) ·
`leveling.py` · `anomaly.py` · `surface.py` · `report_norm.py` ·
`sms_map.py` · `attrs.py` (словник, 47 карток) · `export_points.py`.

**Перевірки:** `перевірки\` — десять скриптів, усі чисті. Запуск:
`runtime\python.exe -X utf8 "перевірки\<назва>.py"`.

## Робочі поля

- **`Зелена_Поляна\Г_03`** — кукурудза 2025, 177,80 га, 222 760 записів.
  Вагова **12 128 кг/га, залікова, на 14 %**. Головне поле.
- `ДЕМО_тестові_дані\Долинка_12` — синтетика.
- `Угринів\Гречка` — сканування RTK + EC.

## План передачі: з чого починати наступну сесію

**Далі по А0-Г.** Батч 15 закрив А0-Г.5 і половину А0-Г.4, батч 17 — А0-Г.2. Порода лишається
та сама: перевірка — або зв'язок — дивиться не туди.

Порядок:

1. **Ревізія точності колонок** — знаки після коми проти справжнього
   діапазону кожної величини. Батч 18 закрив те, що колонки взагалі є в
   таблиці точності; чи правильне саме число знаків — ні. *(А0-Г.3)*
3. **А0-Г.4, залишок** — `_текст_qc`, яку ніхто не кличе (питання до
   Власника), шість непрочитаних змінних `_прогін` і той самий детектор
   мертвих аргументів по решті модулів.
4. **А0-Г.2б** — детектор накладання **посеред** сторінки. Сторожа дивиться
   на курсор у кінці аркуша; картинки його тепер посувають і обійти це
   нічим, але свідомий `set_y` назад вона й далі не побачить.
5. **Б2.2, дрібне** — екран підсумку не показує переліку трьох файлів:
   `підсумок["файли"]` є список, а складальник екрана мовчки пропускає все,
   що не рядок і не число.

**Далі А0-В** — два механізми пошуку аномалій дають різні відповіді в одному
документі: `report_norm.ділянки_уваги` знаходить 4 ділянки на 27 т,
`anomaly.знайти` — жодної. Звести в один.

**А1 стоїть** за рішенням Власника. Коли повернемось — не розбирати картки,
а просити експорт із SMS у shapefile по 2–3 полях, як було з Г-03; і взяти
з ISOXML 28 контурів Svitanok, вони відкриті.

**А3** (повна нормалізація: наскрізне проведення колонок, одиниці,
опитувальник) чекає на А2, а той — на А1.

## Рішення, які не переобговорюються

- **розворотну смугу не чіпаємо** — це врожайність, вона має бути видна;
  техногенне показується окремо;
- **виміряні числа індексами не правимо** — вимір або дійсний, або
  знімається з обліку й перебудовується як пропуск;
- **Swath Width і Distance у SMS = крок сітки**, не жатка;
- **у звіті немає лірики** — числа й карти, без пояснень причин;
- **`YLD_MODEL` — модель, не вимір.** У SMS іде окремим шаром, не
  змішується з фактом;
- методи аналізу — тільки Bray, Olsen, Mehlich-3;
- норми звужені до вапнування;
- на техніку йде тільки латиниця;
- **дані господарств у git не потрапляють ніколи;**
- **на виході рівно три речі: shapefile · PDF · `SMS_import_guide.txt`.**
  Нових файлів у теку віддачі не додавати без прямого прохання Власника;
- **документів не створювати** — ні звітів, ні довідок, ні протоколів, —
  якщо їх не попросили або вони не передбачені порядком роботи.
