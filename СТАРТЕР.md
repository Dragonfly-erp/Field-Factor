# СТАРТЕР — вставити першим повідомленням у нову розмову

Працюємо за системою Progressive Elaboration.

Порядок сесії:

1. Спершу прочитай `C:\WORK D\agroplace-farms\FieldFactor\DAILY_LOG.md` —
   верхній запис.
2. Далі `FieldFactor\STATE.md`, потім `_макети\ПЛАН_ІНТЕРФЕЙСУ.md`, розділ 12.
3. Один батч = одна задача. Не розповзаємось і не забігаємо наперед.
4. Наприкінці — запис у `DAILY_LOG.md` і перезапис `СТАРТЕР.md`.

**Пояснюй простою мовою.** Підсумок наприкінці батча — що з'явилось у
програмі і навіщо це в полі, словами агронома. Назви модулів і механіка
перевірок — у `DAILY_LOG.md`, не в підсумку.

**Головне правило, і воно має два поверхи.** Поки екран не побачений
у браузері — він не зроблений, і клацати треба справжнім клацанням, а не
викликом `.click()` з консолі. Поверхом вище: **зелений результат перевіряє
щасливий шлях, червоний перевіряє перевірку.** Механізм тривоги, якого не
бачили в стані тривоги, — це напис, а не механізм.

**Проєкт.** FieldFactor — портативна програма для агрономічного консалтингу.
Тека `C:\WORK D\agroplace-farms\FieldFactor\`, власний git, репозиторій
`Dragonfly-erp/Field-Factor`. Запуск: `ЗАПУСК.bat` → браузер на
`127.0.0.1:8756`. У `.bat` тільки латиниця. Конфіг запуску для писаря —
`C:\WORK D\agroplace-farms\.claude\launch.json`, запис `FieldFactor`.
Патчі до файлів клади окремим `.py`: bash тут ламається на heredoc
із кирилицею.

**Зламаного немає.** Пройдено руками: зонування, розбір сканування, побудова
зон, `/метод`, `/аналізи` з розкладкою й шаблонами, `/продукти` з формою,
`/карта` з видачею shapefile.

## Що вже стоїть у програмі

**`/метод`** — знімок методу з майстерні: продукти зі складом і CCE, хімічні
еквіваленти, пороги, ворота антагонізму, похибки методу 1σ, статус на кожному
числі. Провенанс: `engine/y8_norms.py` @ `22c6f67` плюс скрипти Зеленої
Поляни. Звірка — 107 чисел, нуль розбіжностей. Джерела **розбираються
деревом, не виконуються**.

**`/аналізи`** — приймання протоколу лабораторії. Кожному числу приписується
метод; «<5», «сліди», порожнє й нуль розрізняються; насиченість основами
рахується самостійно з буферного pH; нумерація лабораторії звіряється з
нашими точками. Видно розкладку колонок, її можна поправити руками й
зберегти шаблоном під лабораторію.

**Фосфор.** Заносимо той метод, який лабораторія зробила: Bray, Olsen або
Mehlich-3. Число не викидається ніколи. Придатність — окремий прапорець:
кислі витяжки на карбонатному ґрунті лишаються у звіті як вимір, але в норму
не заходять. Пороги 7,2 / 7,4 по водному pH погоджено.

**`/продукти`** — довідник із формою: склад партії, нейтралізуюча здатність
(рахується зі складу або береться з протоколу), статус, провенанс. Заготовка
з майстерні лежить тільки для читання; своя партія заводиться поруч і
перекриває її під тим самим іменем. Картку не приймають, якщо складники
дають понад 100 %, карбонат без кальцію, або статус CANON без номера
протоколу.

**`/карта`** — карта внесення: шар зон → норма на кожну зону → продукт →
перевірка → **shapefile або ISO-XML** (сіткою чи полігонами). Усе, що йде на техніку, транслітерується за
КМУ 55-2010; імена колонок ≤ 10 символів і всі різні; пишеться `.cpg`;
пробілів в іменах файлів немає. Перед видачею видно, скільки полігонів,
скільки важитиме файл і скільки продукту вийде разом; за вибором —
порівняння з сіткою в кілограмах. Записане перечитується з диска, і у звіт
іде прочитане, а не задумане.

## Що велике лишилось

| Що | Навіщо |
|---|---|
| **Інтерфейс на сім кроків** | прототип схвалено, у програму не вшито; екрани поки живуть окремо |
| **Геометрія під старі монітори** (наступне) | розчистка дрібних шматків, спрощення меж, мінімальний розмір уздовж руху 30–50 м, буфер 5 м за галочкою |
| **Стокові 20–30 продуктів** | латиницею, з різними типами картки |
| **Сітковий відбір** (у роботі) | ядро зроблено й виміряне, екрана ще немає. Далі: екран, KML, самостійний аналіз рельєфу/вегетації/EC/врожайності |
| **Детектор смугастості проходів** | названо головним руйнівником довіри до карти |
| **Редактор меж зон** | зони не виправити руками |
| **Версійність контуру** | поле змінює межі, історія має лишатись |
| **Перший реліз на GitHub** | механізм оновлення написаний, на справжньому релізі не перевірений |
| **Вапнування** | відкладено Власником — не забігати наперед |

**Дрібні борги:** приймання аналізів із кроку ВХІДНІ · списки вибору в
розкладці відкривати по кліку (тринадцять розгорнутих топлять дерево
доступності) · рівні забезпечення ХАНЗЕ на розфарбування показників ·
скролбари під деревом і в блоках контексту · XLSX не читається, у рантаймі
немає openpyxl.

## Питання, що чекають на Власника

1. **Полігонний `TASKDATA` із SMS** — щоб зняти позначку «не звірено» з
   режиму ISO-XML полігонами. Сітку ми переписали з робочого файлу, полігони
   поки тільки за стандартом.
2. Яких полів бракує в картці продукту.
3. Коли беремо ліцензування й інсталятор — це окремий великий шматок,
   і його немає в списку робіт.

## Рішення, які не переобговорюються

- методи аналізу — тільки Bray, Olsen, Mehlich-3; Чирікова й Мачигіна немає;
- заносимо той метод, який лабораторія зробила; придатність — окремо;
- шкали різних методів між собою не зводяться й не порівнюються;
- склад продукту — з протоколу партії, не з довідника речовин;
- знімок методу з майстерні не правиться з екрана: на ньому тримається звірка;
- норми звужені до вапнування; бенди, балансування, Ca:Mg:K — поза обсягом;
- рівні забезпечення ХАНЗЕ — тільки на розфарбування показників;
- точка = проба, зразок = багато проб або точок;
- карта каже, де брати зразок, а не скільки сипати;
- глибина 30 см — з нашого завдання, не з протоколу лабораторії;
- склад продукту рахуємо **на вологу масу**, як у SMS; поправки на воду немає;
- продукт обирає людина, програма не підбирає;
- формати видачі — тільки ISO-XML і shapefile;
- на техніку йде тільки латиниця; назви господарств і полів у програмі
  лишаються кирилицею;
- не робимо: калібрування монітора по вазі окремим кроком, історію внесення,
  гній.

**Робоче поле:** `робота\Угринів\Гречка` — реальне сканування 10.08.2026,
54,34 га, RTK + EC на 4 362 точки. Аналізів по цьому полю немає. Два
застереження по сканах: мілка й глибока EC майже пропорційні; 14 записів
мають нулі в обох каналах — у розрахунок не пускати.

**Дані господарств у git не потрапляють ніколи.** У `.gitignore`: `робота/`,
`шаблони_лабораторій/`, `довідник_продуктів.json`.
