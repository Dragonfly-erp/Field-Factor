# СТАРТЕР — вставити першим повідомленням у нову розмову

Працюємо за системою Progressive Elaboration.

Порядок сесії:

1. Спершу прочитай `C:\WORK D\agroplace-farms\FieldFactor\DAILY_LOG.md` —
   верхній запис.
2. Далі `FieldFactor\STATE.md`, потім `_макети\ПЛАН_ІНТЕРФЕЙСУ.md`.
3. Один батч = одна задача.
4. Наприкінці — запис у `DAILY_LOG.md` і перезапис `СТАРТЕР.md`.

Головне правило: поки екран не побачений у браузері — він не зроблений.
І клацати треба справжнім клацанням по координатах, а не викликом `.click()`
з консолі: виклик перевіряє функцію, клацання перевіряє інтерфейс.

**Проєкт.** FieldFactor — портативна програма для агрономічного консалтингу.
Тека `C:\WORK D\agroplace-farms\FieldFactor\`, власний git, репозиторій
`Dragonfly-erp/Field-Factor`. Запуск: `ЗАПУСК.bat` → браузер на `127.0.0.1:8756`.
У `.bat` тільки латиниця.

**Зламаного немає.** Вікно зонування, розбір сканування, побудова зон —
пройдено руками.

**Що зараз робимо.** Закриваємо блок **від техніки до техніки**: імпорт із
машини → нормалізація врожайності → зони → завдання на відбір → аналізи →
норма → карта внесення назад на техніку. Інтерфейс переробляється на сім
кроків; прототип каркаса лежить у `_макети\ПРОТОТИП.html`, усі рішення —
в `_макети\ПЛАН_ІНТЕРФЕЙСУ.md`, розділ 12.

**ЗАВДАННЯ 1 — правки до прототипу**, якщо Власник їх назве.

**ЗАВДАННЯ 2 — знімок методу з майстерні в продукт.** Джерело:
`C:\WORK D\agroplace-farms\engine\y8_norms.py` і скрипти Зеленої Поляни
(`farms\Зелена Поляна\2_скрипти\07_карти_врожайності_2026-08-06\y9_lime.py`).
Береться: продукти зі складом і CCE, хімічні еквіваленти, пороги, статуси
CANON/WORKING. Кладеться версія й коміт джерела, щоб потім звірити діф.
Форк дозволено Власником прямо.

**ЗАВДАННЯ 3 — приймання аналізів** під протокол лабораторії ХАНЗЕ:
Bray проти Olsen обирається за pH ґрунту й порівнювати їх заборонено;
K/Ca/Mg/Na — NH₄Ac; мікро — DTPA; бор — гаряча вода; буферний pH за Sikora;
ЄКО; щільність ґрунту з протоколу, якщо є; глибина 30 см з нашого завдання,
не з протоколу; цензуровані значення («<5», «сліди») — окремий тип;
звіряння нумерації лабораторії з нашими точками.

**ЗАВДАННЯ 4 — вапнування** за методом Зеленої Поляни: дефіцит насиченості
основами × ЄКО × маса шару; рішення «вапнувати чи ні» через відстань pH до
порога у σ методу з вироком СТІЙКЕ / НА МЕЖІ / ВИПАДКОВЕ; ворота «кальцій
витісняє калій».

**Рішення, які не переобговорюються:**

- методи аналізу — тільки Bray, Olsen, Mehlich-3; Чирікова й Мачигіна немає;
- норми звужені до вапнування; бенди, балансування, Ca:Mg:K — поза обсягом;
- рівні забезпечення ХАНЗЕ — тільки на розфарбування показників;
- точка = проба, зразок = багато проб або точок;
- карта каже, де брати зразок, а не скільки сипати;
- не робимо: калібрування монітора по вазі окремим кроком, історію внесення.

**У черзі:** сітковий відбір (класичний і динамічний), експорт карти внесення
на термінал (SHP + ISO-XML, ASCII в іменах полів, буфер за межу, розчистка
полігонів), детектор смугастості проходів, редактор меж зон, версійність
контуру, перший реліз на GitHub.

**Робоче поле:** `робота\Угринів\Гречка` — реальне сканування, 54,34 га,
RTK + EC. Дані господарств у git не потрапляють.
