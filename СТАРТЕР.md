# СТАРТЕР — вставити першим повідомленням у нову розмову

Працюємо за системою Progressive Elaboration.

Порядок сесії:

1. Спершу прочитай `C:\WORK D\agroplace-farms\FieldFactor\DAILY_LOG.md` —
   верхній запис.
2. Далі `FieldFactor\STATE.md`, потім `_макети\ПЛАН_ІНТЕРФЕЙСУ.md`, розділ 12.
3. Один батч = одна задача. Не розповзаємось.
4. Наприкінці — запис у `DAILY_LOG.md` і перезапис `СТАРТЕР.md`.

**Головне правило, і воно має два поверхи.** Поки екран не побачений
у браузері — він не зроблений, і клацати треба справжнім клацанням по
координатах, а не викликом `.click()` з консолі: виклик перевіряє функцію,
клацання перевіряє інтерфейс. На цьому ховався баг 19.08. І поверхом вище:
**зелений результат перевіряє щасливий шлях, червоний перевіряє перевірку.**
Механізм тривоги, який не бачили в стані тривоги, — це напис, а не механізм.

**Проєкт.** FieldFactor — портативна програма для агрономічного консалтингу.
Тека `C:\WORK D\agroplace-farms\FieldFactor\`, власний git, репозиторій
`Dragonfly-erp/Field-Factor`. Запуск: `ЗАПУСК.bat` → браузер на
`127.0.0.1:8756`. У `.bat` тільки латиниця — кирилиця ламає розбір cmd.
Конфіг запуску для писаря лежить у `C:\WORK D\agroplace-farms\.claude\launch.json`,
запис `FieldFactor` — у теці продукту свого конфіга немає й не треба.

**Зламаного немає.** Вікно зонування, розбір сканування, побудова зон і
екран методу — пройдено руками.

**Що зараз робимо.** Закриваємо блок **від техніки до техніки**: імпорт із
машини → нормалізація врожайності → зони → завдання на відбір → аналізи →
норма → карта внесення назад на техніку. Інтерфейс переробляється на сім
кроків; прототип каркаса — `_макети\ПРОТОТИП.html`, рішення — в
`_макети\ПЛАН_ІНТЕРФЕЙСУ.md`, розділ 12.

**Зроблено 19.08 уночі: знімок методу з майстерні в продукт.**
`app/core/norms.py` — продукти зі складом і CCE, хімічні еквіваленти, пороги,
ворота антагонізму, похибки методу 1σ, статуси CANON / WORKING / ОСПОРЕНО на
кожному числі. Провенанс: `engine/y8_norms.py` @ `22c6f67`, плюс `y9_flip.py`
і `y9_lime.py` Зеленої Поляни (поза git, звірка за sha256). Звірка з
майстернею — 107 чисел, нуль розбіжностей; тривогу прогнано проти підробленої
копії. Видно на `/метод`, вхід із шапки. **Джерела розбираються деревом, не
виконуються:** `import` скрипта майстерні відкрив і надрукував дані Зеленої
Поляни — так більше не робимо.

**ЗАВДАННЯ 1 — приймання аналізів** під протокол лабораторії ХАНЗЕ:
Bray проти Olsen обирається за pH ґрунту й порівнювати їх між собою
заборонено; K, Ca, Mg, Na — NH₄Ac; мікроелементи — DTPA; бор — гаряча вода;
буферний pH за Sikora; ЄКО; щільність ґрунту з протоколу, якщо вона там є;
глибина 30 см береться з нашого завдання, не з протоколу; цензуровані
значення («<5», «сліди») — окремий тип, не нуль; звіряння нумерації
лабораторії з нашими точками окремим екраном.

**ЗАВДАННЯ 2 — вапнування** за методом Зеленої Поляни: дефіцит насиченості
основами × ЄКО × маса шару; рішення «вапнувати чи ні» через відстань pH до
порога у σ методу з вироком СТІЙКЕ / НА МЕЖІ / ВИПАДКОВЕ; ворота «кальцій
витісняє калій», які скасовують вапнування навіть при низькому pH.
Усі входи для цього вже лежать у `app/core/norms.py`.

**ЗАВДАННЯ 3 — дрібне:** під деревом і в блоках контексту вилазять скролбари.

**Рішення, які не переобговорюються:**

- методи аналізу — тільки Bray, Olsen, Mehlich-3; Чирікова й Мачигіна немає;
- норми звужені до вапнування; бенди, балансування, Ca:Mg:K — поза обсягом;
- рівні забезпечення ХАНЗЕ — тільки на розфарбування показників;
- точка = проба, зразок = багато проб або точок;
- карта каже, де брати зразок, а не скільки сипати;
- не робимо: калібрування монітора по вазі окремим кроком, історію внесення,
  гній.

**У черзі:** сітковий відбір (класичний і динамічний, із коригуванням комірок
на рельєф чи вегетацію); експорт карти внесення на термінал (SHP + ISO-XML,
ASCII в іменах полів DBF, буфер за межу поля, розчистка полігонів, мінімальний
розмір уздовж руху 30–50 м); детектор смугастості проходів; редактор меж зон;
версійність контуру; перший реліз на GitHub.

**Робоче поле:** `робота\Угринів\Гречка` — реальне сканування 10.08.2026,
54,34 га, RTK + EC на 4 362 точки. Два застереження: мілка й глибока EC майже
пропорційні, тож другий канал майже не несе власної інформації; 14 записів
мають нулі в обох каналах — у розрахунок не пускати.

**Дані господарств у git не потрапляють ніколи.** Тека `робота/` у `.gitignore`.
