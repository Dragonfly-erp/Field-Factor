# СТАРТЕР наступної сесії

> Це текст, який Власник вставляє першим повідомленням у нову розмову.
> Він **перезаписується наприкінці кожної сесії** — завжди актуальний.
> Складено: 2026-08-19, вечір.

---

Працюємо за системою **Progressive Elaboration**.

**Порядок сесії:**
1. Спершу прочитай `C:\WORK D\agroplace-farms\FieldFactor\DAILY_LOG.md` — верхній
   запис. Там написано, чого навчила попередня сесія і що зламано.
2. Далі `FieldFactor\STATE.md` — розділ **ЗЛАМАНЕ** стоїть першим.
3. Один батч = одна задача. Не розповзаємось.
4. Наприкінці — запис у `DAILY_LOG.md` (що зроблено · чому · **чого навчився** ·
   проблеми й рішення · план на завтра) і **перезапис `СТАРТЕР.md`** під
   наступну сесію.

**Головне правило, вистраждане вчора:** поки екран не побачений очима у браузері —
він не зроблений. «Написано, але не запущено» — це незакритий пункт, а не помітка
збоку.

---

## Що це за проєкт

**FieldFactor** — портативна програма для агрономічного консалтингу.
Тека `C:\WORK D\agroplace-farms\FieldFactor\`, власний git, репозиторій
`Dragonfly-erp/Field-Factor`. Запуск: `ЗАПУСК.bat` → браузер на `127.0.0.1`.

Бере сирі дані з поля (монітор комбайна, електромагнітне сканування, супутник)
і робить зони керування, файли під SMS Ag Leader і звіт для господарства.

**Правило, яке діє скрізь:** у `.bat` тільки латиниця. Кирилиця живе в даних
і в тому, що читає людина.

---

## ЗАВДАННЯ НА ЦЮ СЕСІЮ

### 1. Полагодити вікно зонування — це перше

Вікно «Зонування поля» відкривається **порожнім**: немає ні цілей
(«ПІД ЩО ЗОНУЄМО»), ні джерел («ЗА ЧИМ РІЖЕМО»). Кнопка каже
«Оберіть хоча б одне джерело», бо обирати нема з чого.

**Що вже виключено — не перевіряй заново:**
- усі функції на місці: `намалювати_джерела` (рядок ~546),
  `намалювати_цілі` (~458), `_картка_сканування`, `_вибір`, `_вгадати_колонку`;
- `ЦІЛІ`, `ПРЕСЕТИ`, `ОПЕРАЦІЇ` оголошені, порядок не заважає;
- контейнери `#цілі` й `#джерела-зонування` у розмітці є;
- `node --check app/web/робоче.js` чистий.

**Висновок:** це виняток під час виконання. Порожні **обидва** розділи, а
`намалювати_джерела` викликається першою — отже падає вона або
`доступні_джерела`.

**Головна підозра:** ключ `колонки` в реєстрі шарів додано пізніше, ніж
записані шари демо-поля. Подивись `робота\ДЕМО_тестові_дані\Долинка_12\шари\шари.json`.

**Перший крок:** запусти програму, відкрий консоль браузера (F12), клацни
операцію зонування, **прочитай перший рядок помилки**. Не гадай.

### 2. Пройти руками показ розбору сканування

Той самий борг: при завантаженні шару типу «сканування» має зʼявитись звіт
про канали й проходи. Написано, але жодного разу не відкрито.

### 3. Далі — дизайн

Власник несе варіанти екранів, зроблені за завданням
`_макети\ЗАВДАННЯ_НА_ДИЗАЙН.md`. Обрати основу й починати переробку
інтерфейсу. До цього — не чіпати оформлення.

---

## Чого НЕ робити

- **Не братись за прив'язку аналізів ґрунту до зон.** Відкладено свідомо
  2026-08-19: у господарствах немає жодного кейсу за цією схемою, перевірятись
  нема на чому.
- **Не «покращувати» піксельний фільтр аномалій EC.** Він навмисне лишений
  попередженням: на перевірці знімав 29 % справжніх слідів і псував 13 %
  чистого поля. Справжню роботу робить вирівнювання по проходах сканера.
- **Не копіювати дані господарств** із `C:\WORK D\agroplace-farms\farms\`
  у теку продукту. Читати на місці — можна.

---

## У черзі, коли головне закрито

- Перший реліз на GitHub і перевірка механізму оновлення на живому теґу —
  досі не перевірений жодного разу.
- Екран «поле в руках» для планшета — поки тільки макет.
